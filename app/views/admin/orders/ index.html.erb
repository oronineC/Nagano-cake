<h2>注文履歴詳細</h2>
<i class="glyphicon glyphicon-plus-sign"></i>
    <!--items一覧 -->
    <table class="table">
      <thead class="thead-light">
        <tr>
          <th>購入日時</th>
          <th>購入</th>
          <th>注文個数</th>
           <th>注文ステータス</th>
        </tr>
      </thead>

      <tbody>
        <% @orders.each do |order| %>
        <tr>
          <td><%= order.created_at %></td>
          <td><%= order.order_item.customer.name.id %></td>
          <td><%= order.order_item.quantiiy %></td>
          <td><%= order.order_status %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
<%= paginate @orders %>