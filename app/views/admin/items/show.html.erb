<h2>商品詳細</h2>
   <%= attachment_image_tag(@item, :image, :fill, 50, 50, fallback: "no_image.jpg")  %>
    <p>商品名</p>
	<p><%= @item.name %></p></br>
	<p>商品説明</p>
	<p><%= @item.explanation%></p></br>
	<p>税込価格</p>
	<p><%= (@item.price* 1.10).round(0)%></p></br>
	<p>(税抜価格)</p>
	<p>(<%= @item.price%>)</p></br>
	<p>ジャンル</p>
	<p><%= @item.genre.name%></p></br>
	<p>ステータス</p>
	<p><%= @item.is_sales_active_i18n %></p></br>
	  <p><%= link_to "Edit", edit_admin_item_path(@item), class: "btn-sm btn-success edit_item_#{@item.id}" %></p>
	  <p><%= link_to "Destroy", admin_item_path(@item), method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger destroy_item_#{@item.id}" %></p>